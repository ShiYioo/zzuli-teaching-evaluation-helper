name: Build and Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Grant execute permission for gradlew
      run: chmod +x kotlin/gradlew

    - name: Build JAR with Gradle
      run: |
        cd kotlin
        ./gradlew jar

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        files: kotlin/build/libs/zzuli-evaluation.jar
        body: |
          ## ZZULI 教学评价助手 ${{ github.ref_name }}
          
          ### 使用方法
          ```bash
          java -jar zzuli-evaluation.jar
          ```
          
          ### 要求
          - Java 21+
          
          ### 登录方式
          - 📱 扫码登录（推荐）
          - 🔑 账号密码登录
          
          ### 日志模式
          - INFO（默认）：简洁输出
          - DEBUG：`java -Dlogback.configurationFile=logback-debug.xml -jar zzuli-evaluation.jar`
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

