<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- 自定义格式，保持 iOS 美学风格 -->
            <pattern>%gray([%level]) %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 文件输出（可选） -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/zzuli-evaluation.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/zzuli-evaluation.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <!-- 如果需要文件日志，取消下面的注释 -->
        <!-- <appender-ref ref="FILE" /> -->
    </root>

    <!-- 应用程序日志级别 -->
    <logger name="AutoEvaluation" level="INFO" />

    <!-- 第三方库日志级别 -->
    <logger name="org.java_websocket" level="WARN" />
</configuration>

